# Archive

Historical phase notes were removed from the working tree after the repo cleanup because they duplicated stale implementation details. Use git history if an old phase plan is needed.

For current setup and development flow, use:

- `README.md`
- `docs/DEVELOPMENT.md`
- `docs/STATUS.md`
